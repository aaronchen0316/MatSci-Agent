# 2026-05-01

## Session Summary
- Implemented MP-backed stability path with local fallback proxy instead of pure fake stability checker.
- Added deterministic `policy_filter` node after retrieval and before prediction.
- Practical policy now excludes actinide/radioactive chemistries and elemental/gas-like candidates for bulk semiconductor-style requests.
- Added offline band-gap benchmark module and runnable script.
- Added tests for stability semantics, policy filtering, benchmark metric helpers, and retriever hull-data propagation.
- Verified suite with `uv run pytest -q`: 27 passed.

## Key Rationale
- Stability should first reuse trustworthy MP hull data when present.
- Policy filter should stay deterministic and avoid extra LLM/API calls.
- Benchmarking should be reusable library + script, not hard-coded into workflow or API.

## Current Risks / Follow-up
- Stability fallback still proxy-based.
- No benchmark result artifact committed yet; only tooling added.
- Policy coverage still narrow and likely needs broader chemistry rules.
